---
import { SERVICES_CONTENT } from '../../../data/our-services-data';
import { ServiceCardIcon } from '../../../components/ui/ServiceCardIcon';
---

<section class="services-section" aria-labelledby="services-title">
  <div class="container">
    <h2 id="services-title" class="section-title">{SERVICES_CONTENT.title}</h2>

    <div class="branches-grid">
      {SERVICES_CONTENT.branches.map((branch) => {
        const services = branch.services;
        const duplicatedServices = [...services, ...services];
        return (
        <div class="branch-column">
          <!-- Branch Header -->
          <div class="branch-header">
            <h3 class="branch-name">{branch.name}</h3>
            <p class="branch-address">{branch.address}</p>
          </div>

          <!-- Service Cards Carousel (infinite scroll) -->
          <div class="carousel-wrapper">
            <div class="carousel-track" role="region" aria-label="Service cards carousel" tabindex="0">
              <div class="carousel-inner" style={`width: ${duplicatedServices.length * 100}%; --card-count: ${duplicatedServices.length}`}>
                {duplicatedServices.map((service, i) => (
                  <div class="service-card" data-index={i}>
                    <div class="card-content">
                      <div class="icon-wrapper">
                        <ServiceCardIcon name={service.iconName} size={88} />
                      </div>
                      <h4 class="service-title">{service.title}</h4>
                    </div>
                  </div>
                ))}
              </div>
            </div>

            <div class="services-dots" role="tablist" aria-label={`Service cards for ${branch.name}`}>
              {services.map((_, i) => (
                <button
                  type="button"
                  class={`dot ${i === 0 ? 'active' : ''}`}
                  role="tab"
                  aria-label={`Service ${i + 1}`}
                  aria-selected={i === 0}
                  data-index={i}
                />
              ))}
            </div>
          </div>
        </div>
        );
      })}
    </div>
  </div>
</section>

<style>
  /* COLORS & TOKENS */
  :root {
    --color-bg-cream: #ede4c5;
    --color-card-beige: #ddd2b0;
    --color-text-green: #0a441e;
    --color-text-gold: #bb9313;
    --color-arrow-bg: #c9bf9e;
  }

  .services-section {
    background-color: var(--color-bg-cream);
    padding: 5rem 1.5rem;
    font-family: 'Montserrat', sans-serif;
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
  }

  .section-title {
    font-family: 'Calistoga', serif;
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--color-text-green);
    text-align: center;
    margin: 0 0 4rem 0;
    line-height: 1.3;
  }

  .branches-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
  }

  @media (min-width: 768px) {
    .branches-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 3rem;
    }
  }

  .branch-column {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .branch-header {
    text-align: center;
    margin-bottom: 2rem;
    min-height: 80px;
  }

  .branch-name {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-text-gold);
    margin: 0 0 0.25rem 0;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .branch-address {
    font-size: 1.1rem;
    font-weight: 400;
    color: var(--color-text-gold);
    margin: 0;
    line-height: 1.6;
    white-space: pre-line;
    text-align: center;
  }

  .carousel-wrapper {
    position: relative;
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
  }

  .carousel-track {
    overflow-x: auto;
    overflow-y: hidden;
    border-radius: 24px;
    height: 280px;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .carousel-track::-webkit-scrollbar {
    display: none;
  }

  .carousel-inner {
    display: flex;
    height: 100%;
  }

  .carousel-inner .service-card {
    flex: 0 0 calc(100% / var(--card-count, 16));
    min-width: calc(100% / var(--card-count, 16));
    scroll-snap-align: start;
    scroll-snap-stop: always;
  }

  .service-card {
    font-family: 'Montserrat', sans-serif;
    background-color: var(--color-card-beige);
    border-radius: 24px;
    padding: 3rem 2rem;
    height: 280px;
    min-height: 280px;
    display: flex;
    justify-content: center;
    align-items: center;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    box-sizing: border-box;
  }

  .card-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    text-align: center;
  }

  .icon-wrapper {
    width: 88px;
    height: 88px;
    color: var(--color-text-green);
  }

  .icon-wrapper :global(svg) {
    width: 100%;
    height: 100%;
  }

  .service-title {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-text-green);
    margin: 0;
    line-height: 1.35;
    white-space: pre-line;
  }

  .carousel-track {
    touch-action: pan-x pan-y pinch-zoom;
  }

  .services-dots {
    display: flex;
    gap: 10px;
    margin-top: 1rem;
    justify-content: center;
    align-items: center;
  }

  .services-dots .dot {
    width: 12px;
    height: 12px;
    border-radius: 999px;
    border: none;
    padding: 0;
    cursor: pointer;
    background-color: var(--color-text-gold);
    opacity: 0.35;
    transition: opacity 0.2s, transform 0.2s;
  }

  .services-dots .dot[aria-selected="true"],
  .services-dots .dot.active {
    opacity: 1;
    transform: scale(1.05);
  }
</style>

<script>
  document.querySelectorAll('.branch-column').forEach((column) => {
    const trackEl = column.querySelector('.carousel-track') as HTMLElement | null;
    const inner = column.querySelector('.carousel-inner') as HTMLElement | null;
    const cards = column.querySelectorAll('.carousel-inner .service-card');
    const dots = column.querySelectorAll('.services-dots .dot') as NodeListOf<HTMLButtonElement>;

    if (!trackEl || !inner || cards.length === 0) return;
    const track = trackEl;

    const totalCards = cards.length;
    const realCount = Math.max(1, Math.floor(totalCards / 2));
    let oneSetWidth = 0;
    let ticking = false;
    let autoTimer: ReturnType<typeof setInterval> | null = null;

    function measure() {
      oneSetWidth = track.scrollWidth / 2;
    }

    function getRealIndexFromScroll() {
      const slideWidth = track.clientWidth || 1;
      const approx = Math.round(track.scrollLeft / slideWidth);
      return ((approx % realCount) + realCount) % realCount;
    }

    function updateDots() {
      if (!dots || dots.length === 0) return;
      const active = getRealIndexFromScroll();
      dots.forEach((dot, i) => {
        dot.setAttribute('aria-selected', i === active ? 'true' : 'false');
        dot.classList.toggle('active', i === active);
      });
    }

    function wrapScroll() {
      if (oneSetWidth <= 0) measure();
      const left = track.scrollLeft;
      if (oneSetWidth <= 0) {
        ticking = false;
        return;
      }
      if (left >= oneSetWidth - 2) {
        track.scrollLeft = left - oneSetWidth;
      } else if (left <= 0) {
        track.scrollLeft = left + oneSetWidth;
      }
      updateDots();
      ticking = false;
    }

    function onScroll() {
      if (!ticking) {
        requestAnimationFrame(wrapScroll);
        ticking = true;
      }
    }

    track.addEventListener('scroll', onScroll, { passive: true });

    function onWheel(e: WheelEvent) {
      if (oneSetWidth <= 0) return;
      const left = track.scrollLeft;
      if (left <= 1 && e.deltaX > 0) {
        track.scrollLeft = oneSetWidth;
        e.preventDefault();
      } else if (left >= oneSetWidth - 1 && e.deltaX < 0) {
        track.scrollLeft = left - oneSetWidth;
        e.preventDefault();
      }
    }

    track.addEventListener('wheel', onWheel, { passive: false });

    function goToRealIndex(realIndex: number, behavior: ScrollBehavior = 'smooth') {
      const slideWidth = track.clientWidth || 0;
      const target = Math.max(0, Math.min(realCount - 1, realIndex)) * slideWidth;
      track.scrollTo({ left: target, behavior });
      updateDots();
    }

    function next() {
      const slideWidth = track.clientWidth || 0;
      if (slideWidth <= 0) return;
      track.scrollBy({ left: slideWidth, behavior: 'smooth' });
    }

    function startAuto() {
      if (autoTimer) return;
      autoTimer = setInterval(() => {
        if (!document.hidden) next();
      }, 3000);
    }

    function stopAuto() {
      if (!autoTimer) return;
      clearInterval(autoTimer);
      autoTimer = null;
    }

    track.addEventListener('mouseenter', stopAuto);
    track.addEventListener('mouseleave', startAuto);
    track.addEventListener('focusin', stopAuto);
    track.addEventListener('focusout', startAuto);
    track.addEventListener('pointerdown', stopAuto, { passive: true });
    track.addEventListener('pointerup', startAuto, { passive: true });
    track.addEventListener('touchstart', stopAuto, { passive: true });
    track.addEventListener('touchend', startAuto, { passive: true });

    dots?.forEach((dot) => {
      dot.addEventListener('click', () => {
        const idx = Number(dot.dataset.index ?? '0');
        stopAuto();
        goToRealIndex(idx, 'smooth');
        startAuto();
      });
    });

    const ro = new ResizeObserver(() => {
      measure();
      updateDots();
    });
    ro.observe(track);
    measure();
    updateDots();
    startAuto();
  });
</script>
