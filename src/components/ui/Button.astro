---
interface Props {
  variant?: 'primary' | 'secondary' | 'outline' | 'white';
  className?: string;
  class?: string;
  href?: string;
}

const { variant = 'primary', className = '', class: classProp = '', href } = Astro.props;
const combinedClass = className || classProp;
const { COLORS } = await import('../../constants');

const variantStyles = {
  primary: { backgroundColor: COLORS.blue.royal, color: 'white' },
  secondary: { backgroundColor: COLORS.green, color: COLORS.blue.navy },
  outline: { border: `2px solid ${COLORS.blue.royal}`, color: COLORS.blue.royal },
  white: { backgroundColor: 'white', color: COLORS.blue.royal },
};

const baseStyle = "inline-flex items-center justify-center px-6 py-3 rounded-full font-bold transition-all active:scale-95 shadow-md";
const Tag = href ? 'a' : 'button';
---

<Tag
  class={`${baseStyle} ${variant === 'white' ? 'hover:bg-gray-100' : 'hover:brightness-110'} ${combinedClass}`}
  style={variantStyles[variant]}
  href={href}
>
  <slot />
</Tag>
